import React from 'react';

type AcceptedProps = {
   
}

class About extends React.Component <{}, AcceptedProps>{
    constructor(props: AcceptedProps) {
        super (props)
    }

    render () {
        return (
        <div className="About">
            
        </div>
        );
    }
}

export default About;