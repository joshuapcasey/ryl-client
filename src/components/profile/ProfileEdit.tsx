import React from 'react';

type AcceptedProps = {
   
}

class ProfileEdit extends React.Component <AcceptedProps, {}> {
    constructor(props: AcceptedProps) {
        super (props)
    }

    render () {
        return (
        <div className="ProfileEdit">
            
        </div>
        );
    }
}

export default ProfileEdit;