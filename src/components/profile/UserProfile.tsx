import React from 'react';

type AcceptedProps = {
   
}

class UserProfile extends React.Component <AcceptedProps, {}> {
    constructor(props: AcceptedProps) {
        super (props)
    }

    render () {
        return (
        <div className="UserProfile">
            
        </div>
        );
    }
}

export default UserProfile;