import React from 'react';

type AcceptedProps = {
   
}

class ProfileIndex extends React.Component <AcceptedProps, {}> {
    constructor(props: AcceptedProps) {
        super (props)
    }

    render () {
        return (
        <div className="ProfileIndex">
            
        </div>
        );
    }
}

export default ProfileIndex;