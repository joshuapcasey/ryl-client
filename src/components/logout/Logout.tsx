import React from 'react';

type AcceptedProps = {
   
}

class Logout extends React.Component <AcceptedProps, {}> {
    constructor(props: AcceptedProps) {
        super (props)
    }

    render () {
        return (
        <div className="Logout">
            
        </div>
        );
    }
}

export default Logout;